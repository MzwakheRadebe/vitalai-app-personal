# Python artifacts
__pycache__/
*.py[cod]
*.egg-info/

# Environment and secrets — never commit real credentials
.env
.env.local
.env.production
.env.development
# Allow .env.example (safe template with no real values)
!.env.example

# Local dev database
local_offline.db

# Test caches
.pytest_cache/

# IDE/editor (JetBrains)
shelf/
workspace.xml
httpRequests/
dataSources/
dataSources.local.xml

# IDE/editor
.vscode/
.DS_Store

# Node
node_modules/
frontend/build/

# Frontend environment files (contain API URLs, never commit production URLs)
frontend/.env
frontend/.env.local
frontend/.env.production
frontend/.env.development
!frontend/.env.example

# ML model artifacts (large binary files — regenerated on first start)
ai_and_nlp/severity_classifier.joblib
ai_and_nlp/embedder_model/