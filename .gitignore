# Python artifacts
__pycache__/
*.py[cod]
*.egg-info/

# Environment and secrets — never commit real credentials
.env
.env.local
.env.production
.env.development
# Allow .env.example (safe template with no real values)
!.env.example

# Local dev database
local_offline.db

# Test caches
.pytest_cache/

# IDE/editor (JetBrains)
shelf/
workspace.xml
httpRequests/
dataSources/
dataSources.local.xml

# IDE/editor
.vscode/
.DS_Store

# Node (if frontend added later)
node_modules/